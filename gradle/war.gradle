apply plugin: "war"

bootWar {
    mainClassName = "com.kepco.etax.openapi.KepcoEtaxApiApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
